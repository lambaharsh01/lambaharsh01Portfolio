export interface ProjectDataInterface {
    projectName: string;
    category: string;
    cardSize: string;
    image: string;
    link: string;
}
export interface ProjectCardInterface{
    data: ProjectDataInterface
}